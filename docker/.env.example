# Database — primary (kodexa_hotel = identity + CMS + custom emulator)
MYSQL_ROOT_PASSWORD=root_password_change_me
MYSQL_DATABASE=kodexa_hotel
MYSQL_USER=kodexa
MYSQL_PASSWORD=kodexa_pass_change_me

# Redis
REDIS_PASSWORD=

# Ports (change if conflicts)
DB_PORT=3306
REDIS_PORT=6379
ADMINER_PORT=8080
REDIS_INSIGHT_PORT=8001
IMAGER_PORT=1338

# ── Future: Arcturus databases (created empty by MP-007, not yet in use) ──────
# arcturus_main — Hotel Principal (ObjectRetros import pending Diego approval)
# arcturus_dev  — Desarrollo lab (nitro-docker / dev import pending)
# Both live in the same MariaDB container as kodexa_hotel.
# Add dedicated user/password when Arcturus is active:
# ARCTURUS_DB_USER=arcturus_user
# ARCTURUS_DB_PASSWORD=change_me_in_production
